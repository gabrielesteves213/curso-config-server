spring:
  application:
    name: greeting-service

greeting-service:
    greeting: "Olá"
    default-value: "Mundo HOMOL"
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'